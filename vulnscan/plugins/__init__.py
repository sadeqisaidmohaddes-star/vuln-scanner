"""User plugin package — drop additional ScannerModule subclasses here.

Any ``*.py`` module in this package (or in a directory passed via ``--plugins-dir``)
that defines a concrete :class:`~vulnscan.core.module_base.ScannerModule` subclass
is loaded automatically at runtime. See ``README.md`` in this directory.
"""
