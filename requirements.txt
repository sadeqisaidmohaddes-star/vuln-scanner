# Runtime dependencies for vulnscan (Python 3.11+)
httpx>=0.27,<1.0          # async HTTP client for header/exposure/injection checks
dnspython>=2.6,<3.0       # DNS records, AXFR attempts, subdomain enumeration
PyYAML>=6.0,<7.0          # scope file parsing (YAML/JSON)
colorama>=0.4.6,<0.5      # cross-platform (Windows) ANSI colors for console output
cryptography>=42.0        # X.509 certificate parsing for the TLS module

# --- Development / testing (install with: pip install -r requirements.txt -r requirements-dev.txt) ---
# pytest>=8.0
# pytest-asyncio>=0.23
